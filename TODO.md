# 하루매일 TODO

순서 원칙: **UI 모양을 한 구조 작업(스키마·날짜 계산·격자 렌더링)은 앞에, 겉모습 다듬기는 P5에.**
겉모습은 기능이 붙을 때마다 레이아웃이 바뀌어서 일찍 다듬으면 두 번 일한다.
단, 매일 쓰다가 너무 불편한 부분은 P5에서 당겨와도 된다.

## 완료

- [x] 월간 달력 — 42칸 격자, 오늘·주말 색, 달 이동, 날짜 선택, 시각 표시
- [x] 바탕화면 위젯 — 아이콘 뒤 부착, 위치 조정 모드(드래그·크기 조절·저장), 크기별 자동 스케일
- [x] 로컬 저장(SQLite) — 일정·할 일 추가/삭제/완료, 메인↔위젯 실시간 반영
- [x] 구글 캘린더 읽기 — OAuth(PKCE), 토큰은 자격 증명 관리자, 이 달 가져오기, 반복 일정 펼침
- [x] 구글 할 일 읽기 — 모든 목록, 마감일 있는 것만 달력에 일정 모양으로 표시
- [x] 배포 기반 — 앱 게시(프로덕션), 홈페이지·개인정보처리방침 (GitHub Pages)

## P0 정리

- [x] 미커밋 변경 커밋
- [x] README를 Svelte 템플릿 기본값에서 교체

## P1 데이터 뼈대

- [x] **1. 캘린더/목록 구분 스키마** (이름·색) — 마이그레이션 v2
  - `calendars(id, name, color)` + `items.calendar_id` (캘린더 지우면 항목도 삭제)
  - 구글 캘린더는 구글이 준 색, 할 일 목록은 목록 id로 고정 색, 공휴일은 초록
  - `items.at` NULL 허용으로 바꿈 → P2-9에서 날짜 없는 할 일을 담을 수 있음. **앱 쪽 저장(`db.ts` write)은 아직 막아둠**
  - 마이그레이션 때 구글 캐시는 비움 → "이 달 가져오기" 한 번 다시 눌러야 함
- [x] 2. 구글 할 일 읽기
  - [ ] 마감 지난 미완료 할 일을 어디에 보여줄지 규칙 (처음부터 미결) → P2-9와 같이 결정
- [x] **3. 일정별 색 반영** — 구글에서 일정마다 지정한 색이 앱에서도 같게
  - 일정의 `colorId`(1~11)를 받아 `colors.get` 팔레트로 색을 정함. `colorId`가 없으면 캘린더 기본 색
  - 마이그레이션 v3 `items.color`. 항목 자기 색(`color`)과 캘린더 색(`calendarColor`)을 따로 들고 다님
  - 2026-09-17 지형님 화면에서 확인 완료
- [ ] ~~여러 캘린더 읽기~~ → **보류** (2026-09-16)
  - 지형님 구성: 조지형(기본, 읽는 중) / Tasks(할 일 API로 이미 읽음) / 생일(안 읽음) / 대한민국의 휴일(천문연구원 공휴일로 대체)
  - 다시 볼 때: 생일이 필요해지거나 다른 사람이 쓸 때. 준비는 돼 있음(일정 id `gcal:<캘린더>:<일정>`, 캘린더 이름·색은 calendarList)
  - ⚠️ "대한민국의 휴일" 같은 공휴일 구독 캘린더는 빼야 함 — 안 그러면 공휴일이 두 번 뜸
- [ ] **4. 자동 동기화** (2026-09-17 설계 확정)
  - 언제 가져오나
    1. **앱 켤 때** — 이번 달
    2. **달 이동할 때** — DB에 있는 걸 먼저 바로 보여주고 동기화는 뒤에서. 빠르게 여러 번 넘기면
       **멈추고 0.5초 뒤에만** 가져오고, 최근 몇 분 안에 가져온 달이면 건너뜀
    3. **켜둔 동안 15분마다** — 폰에서 바꾼 게 위젯에 반영되도록. 위젯은 달을 안 넘기니 이게 없으면 계속 옛날 데이터
       - 범위: 위젯이 보는 이번 달 + 메인 창이 다른 달을 보고 있으면 그 달도
  - 규칙
    - **자동 동기화 실패는 조용히** — 빨간 에러 대신 기존 데이터 유지 + "마지막 동기화 N분 전" 표시
      - [x] 표시 부분은 먼저 구현 (2026-09-17): `마지막 동기화: 3분 전 (일정 n건, 할 일 m건 완료)`, 30초마다 갱신, 앱 다시 켜도 유지(localStorage), 연결 해제 시 지움
    - **동시에 두 번 안 돌게** — 돌고 있으면 다음 것은 기다림 (켜자마자 달 넘기면 겹쳐서 지우고 넣기가 꼬일 수 있음)
    - 수동 버튼은 "지금 새로고침" 용도로 남김
    - 동기화는 메인 창에서 돈다 (DB 쓰기 권한이 메인 창에만 있음). 트레이 상주(P4)로 창을 숨겨도 계속 돌아야 함
  - 비용: 동기화 1번에 구글 요청 7~8번 → 15분 주기면 시간당 30번 정도, 할당량 문제 없음
  - 공휴일은 지금처럼 실행당 해마다 한 번 (이 주기와 무관)
- [ ] 5. 위젯 날짜 갱신 버그 — 켤 때의 달에 고정, 자정·월말을 넘기면 안 바뀜
  - **P1-4의 15분 타이머로 같이 처리** — 깨어날 때 날짜가 바뀌었으면 위젯의 오늘·달도 갱신

## P2 구조

- [x] 6. 주 시작 요일 설정 (일요일/월요일) — 메인 창 상단 선택, 위젯에도 바로 반영(localStorage)
  - ponytail: 설정 화면(P5) 생기면 그리로 옮길 것
- [x] 7. 여러 날 일정을 띠로 — 주 단위로 막대 배치, 주 경계를 넘으면 끝을 평평하게 이어 그림
  - 날짜 칸 클릭 시 가운데 날짜에서도 그 일정이 목록에 나옴
- [x] 8a. 공휴일 — 한국천문연구원 특일정보 API, 실행당 해마다 한 번 받아 DB에 캐시
  - **서비스 키 필요**: `src-tauri/holiday-api-key.txt` (README 참고)
- [ ] 8b. 음력
- [ ] **9. 오른쪽 목록 패널** — 아래 메모 참고
- [x] **9a. 사이드 뷰(Day Panel) — 선택된 날짜 표시** (2026-09-17)
  - 순서: 날짜 → 공휴일 → 종일 일정 → 할 일 전부 → 구분선 → 시각 있는 일정·할 일(시작 순)
  - 시각 있는 할 일은 할 일 목록과 시각 스택에 같은 객체로 중복 표시 (의도)
  - 원본은 `items.svelte.ts`의 `itemStore` 하나, 격자·패널이 같은 배열을 참조
  - 종일 여부를 DB에 저장(마이그레이션 v4 `all_day`) — 자정 시작 어림 대신 구글 `start.dateTime` 유무
  - 메인 창·위젯 둘 다. 위젯은 반투명 배경 + 글자 그림자
  - 지금은 **오늘 고정**. 다른 달로 넘겨도 오늘 항목이 있게 읽는 구간에 오늘을 포함
  - 스펙과 다르게 한 것: 공휴일은 구글 휴일 캘린더 ID가 아니라 `holiday:kr`(천문연구원)로 구분.
    생일은 캘린더를 안 읽어서(여러 캘린더 보류) 비어 있음
  - [ ] 다음: 월 격자에서 날짜 누르면 패널 날짜 바뀌기 (기본값 오늘)
  - [ ] 다음: 체크박스로 할 일 완료 (낙관적 업데이트 + 실패 시 롤백). 구글 할 일은 양방향(P3-11)이 있어야 실제로 반영됨
  - [ ] 기존 아래쪽 날짜 편집 패널과 역할이 겹침 — 날짜 클릭 연동할 때 합칠지 정할 것
  - 참고: 월 격자에서 시각 있는 할 일은 아직 시각이 안 붙음 (격자는 일정만 시각 표시)

## P3 큰 기능

- [ ] 10. 주간 뷰
- [ ] 11. 양방향 — 앱에서 만든/바꾼 일정·할 일을 구글에 쓰기
  - 할 때 개인정보처리방침의 "가져와 표시합니다" 문구도 수정 (구글 정책)
  - 지금은 구글 항목의 체크·삭제를 편집 패널에서 막아둠

## P4 상주 앱

- [ ] 12. 트레이 상주 (창 닫아도 안 꺼짐)
- [ ] 13. 윈도우 시작 시 자동 실행
- [ ] 14. 증분 동기화 (캘린더 syncToken, 할 일은 updatedMin 정도만 가능)

## P4.5 설정 저장 위치

- [ ] 주 시작 요일·마지막 동기화가 localStorage에 있어서 **개발 빌드와 릴리스 빌드가 설정을 공유하지 않는다**
      (주소가 http://localhost:1420 과 tauri://localhost 로 달라 저장소가 분리됨).
      설정 화면(P5) 만들 때 SQLite나 설정 파일로 옮길 것

## P5 UI 다듬기

- [ ] 헤더 레이아웃 (월간/주간 토글 가운데, 아이콘 버튼)
- [ ] 일정 편집 패널 디자인
- [ ] 구글 연동 영역 → 설정 화면으로 분리
- [ ] 색·폰트·간격, 라이트 모드
- [ ] 앱 아이콘 (지금 Tauri 기본)

## P6 배포 (결정 후)

- [ ] 설치 파일, 코드 서명
  - 글꼴 라이선스: Pretendard(OFL 1.1) 라이선스 전문을 설치 파일에 같이 넣을 것 (`node_modules/pretendard/dist/LICENSE.txt`). 빌드 CSS는 주석이 지워져서 저작권 표기가 안 남는다
- [ ] 구글 앱 검증 — 사용자 100명 넘을 때. 스코프를 좁힐지(`.readonly`) 양방향 여부 보고 결정
- [ ] 공용 도메인(github.io)이 검증에서 걸리면 도메인 구입

---

## 메모: 오른쪽 목록 패널 (2026-09-16 요청, 구현 보류)

**요청** — 날짜·시간이 정해진 할 일은 달력에 잘 뜨는데, 정해지지 않은 할 일은 안 보인다.
달력 오른쪽에 **할 일 목록**과 **일정 목록**을 나열하는 리스트로 보여주면 좋겠다.

**구현할 때 걸리는 것**

- 지금은 마감 없는 할 일을 **가져오지도 않음** — `google.rs`의 `to_remote_task`가 버리고, 조회도 `dueMin`/`dueMax`로 거름. 마감 없는 것을 받는 조회 경로가 따로 필요
- DB `items.at`이 NOT NULL → 날짜 없는 항목을 저장하려면 스키마 변경. **P1-1 스키마 작업 때 같이 설계**
- "마감 지난 미완료 할 일" 규칙과 한 번에 정하는 게 좋음 — 둘 다 "달력 칸에 안 맞는 할 일을 어디 보여주나"의 문제
- 할 일 목록을 구글 목록별로 묶을지 → P1-1 목록 구분과 연결
- 위젯에도 보여줄지 (위젯은 지금 격자만)
- 일정 목록은 무엇을 나열할지 (이번 달 전체? 오늘 이후? 선택한 날짜?) 정해야 함
